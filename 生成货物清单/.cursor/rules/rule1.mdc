---
alwaysApply: true
---
This is an AirScript project in WPS.

The document begins with a version number, which must be updated according to the following rule:

1. 仅当“本对话中发生了代码改动（已实际修改并保存到文件，无需等提交）”时，才更新版本号；如果只是讨论/询问但未改代码，则不更新版本号。
2. 版本号写在文件最开头一行，格式为：`//vX.Y.Z`
3. 当进入 beta 测试时，将版本号从 `//vX.Y.Z` 更新为：`//vX.Y.(Z+1)-beta.1`
4. 当已是 beta 版本时，将版本号从 `//vX.Y.Z-beta.N` 更新为：`//vX.Y.Z-beta.(N+1)`
5. 当你手动删除 `-beta.N` 后，版本号回到 `//vX.Y.Z`；下一次发生代码改动且版本号不含 `-beta` 时，再次从 `-beta.1` 进入（Z 使用当前版本号的 Z）。